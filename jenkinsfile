pipeline {
    agent any
    
    tools {
        maven 'Maven3'
        jdk 'JDK25'
    }
    
    stages {
        stage('Build') {
            steps {
                sh 'mvn clean install'
            }
        }
    }
    
    post {
        success {
            echo 'Build successful!'
        }
        failure {
            echo 'Build failed!'
            emailext (
                to: 'akainusan555@gmail.com',
                subject: "Build Failed: ${env.JOB_NAME} - ${env.BUILD_NUMBER}",
                body: "Check the build logs at ${env.BUILD_URL}"
            )
        }
    }
}